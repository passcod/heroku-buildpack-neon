version: '2'

services:
  testbuild:
    image: 'heroku/heroku:16-build'
    command:
      - bin/compile
      - /src/neon-test
      - /src/cache
    environment:
      HOME: /src
      TRAVIS: "$TRAVIS"
      CI: "$CI"
    network_mode: host
    user: "$UID:$GROUPS"
    volumes:
      - '.:/src'
      - '../bin:/src/bin'
    working_dir: '/src'

