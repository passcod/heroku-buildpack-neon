[package]
name = "neon-test"
version = "0.1.0"
authors = ["Félix Saparelli <felix@passcod.name>"]
license = "MIT"
build = "build.rs"

[lib]
name = "neon_test"
crate-type = ["dylib"]

[build-dependencies]
neon-build = "0.1.15"

[dependencies]
neon = "0.1.15"
